export type RootStackParamList = {
    Home: { id: string };
    Reader: { id: number };
}

export type HomeTabParamList = {
    Main: { id: string };
    Collection: { id: string };
    History: { id: string };
    Setting: { id: string };
}